<view class="tabs">
  <view class="tabs_title">
    <view wx:for="{{tabs}}" class="tabs_item {{item.isActive?'active':''}}" bindtap="handleItemTap" data-index="{{index}}">
      {{item.value}}
    </view>
  </view>
  <view class="content">
    <slot></slot>
  </view>
</view>